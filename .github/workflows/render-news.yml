name: Render review-first news

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - 'news/news-data.json'
      - 'news/index.html'
      - 'scripts/render_news.py'
      - 'scripts/integrate_news.py'
      - 'scripts/remove_visible_sources.py'
      - 'scripts/update_accommodation_currency.py'
      - 'index.html'
      - 'xjtlu-accommodation-sip-taicang.html'
      - 'sitemap.xml'
      - '.github/workflows/render-news.yml'

permissions:
  contents: write

jobs:
  render-news:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout triggering branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Restore news and enforce detail-page policies
        run: |
          python scripts/remove_visible_sources.py
          python scripts/integrate_news.py --prepare
          python scripts/render_news.py
          python scripts/integrate_news.py --finalize
          python scripts/update_accommodation_currency.py
          python scripts/remove_visible_sources.py --check
          python scripts/integrate_news.py --check
          git diff --check

      - name: Commit generated files to the same branch
        run: |
          if [ -z "$(git status --porcelain -- index.html sitemap.xml news xjtlu-*.html)" ]; then
            echo "Generated files are already current"
            exit 0
          fi
          git config user.name "TNS News Renderer"
          git config user.email "actions@users.noreply.github.com"
          git add index.html sitemap.xml news xjtlu-*.html
          git commit -m "Render XJTLU Korea news and site updates"
          git push origin "HEAD:${GITHUB_REF_NAME}"
