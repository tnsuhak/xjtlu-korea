# XJTLU Korea 뉴스 운영 규칙

## 처리 흐름

1. `tns-site-manager`가 XJTLU 공식 뉴스 목록을 정기 확인한다.
2. 새 URL을 `data/news_candidates.json`에 `pending`으로 저장한다.
3. 편집 자동화는 한국 학생·학부모에게 실질적으로 관련된 항목만 고른다.
4. 한국어 요약 초안을 별도 브랜치의 `news/news-data.json`에 추가한다.
5. GitHub Actions가 뉴스 기사·목록·홈페이지 최신 뉴스·sitemap을 정적으로 생성한다.
6. PR과 Netlify Deploy Preview에서 사람이 사실·표현·출처를 검토한다.
7. 사용자가 승인한 PR만 `main`에 병합한다. 자동화는 운영 브랜치를 직접 수정하거나 병합하지 않는다.

## 자동 초안 허용 범위

- 입학조건, 지원 일정, 학비, 장학금
- 전공 신설·변경, 학위, 리버풀대학교 협력
- 국제학생에게 중요한 캠퍼스·교육·취업 지원
- 대학 공식 랭킹·인증·연구 소식 중 한국 지원자에게 의미가 큰 항목

## 자동 초안 제외 범위

- 이름이나 개인 서사가 중심인 학생·졸업생·교직원 사례
- 사용 허가 여부를 확인할 수 없는 인터뷰·후기·사진
- 학교 공식 뉴스가 아닌 2차 기사만 존재하는 내용
- 제목만으로 의미를 판단할 수 없거나 수치·조건을 원문에서 확인할 수 없는 내용
- 한국 지원자에게 직접적인 정보 가치가 낮은 행사·내부 소식

## 필수 데이터

- `id`: 영문 소문자 slug
- `title`: 자연스러운 한국어 제목
- `summary`: 목록과 기사 상단에 쓰는 2~3문장 핵심 요약
- `date`: 한국어 요약 초안 작성일(YYYY-MM-DD)
- `source_date`: 공식 원문 게시일
- `source_url`: `https://www.xjtlu.edu.cn/en/news/` 아래의 공식 상세 주소
- `category`: 입학·장학금·전공·진로 등
- `status`: 프리뷰는 `preview_ready`, 승인 후 `approved` 또는 `published`
- `content_type`: `official_update`
- `personal_story`: 반드시 `false`
- `sections`: 소제목과 문단 배열
- `reviewed_at`: 사람 검토일

## 노출 순서 규칙

- 메인 화면의 최신 뉴스와 `/news/` 목록은 TNS 작성일이 아니라 **XJTLU 공식 원문 게시일(`source_date`) 최신순**으로 정렬한다.
- 새 기사를 뒤늦게 추가해도 오래된 원문이 최신 기사보다 위로 올라오지 않도록 한다.
- 같은 원문 게시일일 때만 `priority`를 보조 정렬값으로 사용한다.
- 카드에 표시하는 날짜도 `source_date`를 사용한다.

## 편집 원칙

- 완전 번역이 아닌 출처 기반의 독자적인 한국어 기사로 정리한다.
- 기사 본문은 보통 3~5개 소제목, 5~8개 문단으로 구성해 배경·핵심 내용·한국 학생에게 의미 있는 점을 충분히 설명한다.
- 2~3문단으로 지나치게 압축한 기사보다는, 원문에서 확인되는 중요한 사실과 맥락을 빠뜨리지 않는 편집을 우선한다.
- 목록용 `summary`는 짧게 유지하되 기사 본문까지 짧게 줄이지 않는다.
- 공식 발표에 없는 평가·보장·추정을 추가하지 않는다.
- 원문 날짜, 작성일, 최종 검토일을 구분한다.
- 모든 기사에 공식 원문 링크와 TNS 편집 콘텐츠라는 설명을 표시한다.
- 기사 본문은 정적 HTML로 생성해 검색엔진과 AI 검색이 읽을 수 있게 한다.
